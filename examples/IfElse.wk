main {
    string name
    int age
    
    PRINT "What is your name?"
    INPUT name
    
    PRINT "What is your age?"
    INPUT age
    
    if ?age and age > 0 {
        PRINT "Welcome "
        PRINT name
    } else {
        PRINT "You are not a real person!"
    }
    
    if age < 0 {
    	PRINT "Unmatched age!"
    }
}
