import [iostream, sensors]

main {    
    number temperature
    await temperature = sensors.temperature.read()
    
    for !?temperature {
        println("Still waiting")
    }
    
    println("The temperature is: " + temperature)
}
